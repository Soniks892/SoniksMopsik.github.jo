import turtle
t = turtle.Pen()
def figura(t, filled):
	if filled == True:
		t.begin_fill()
	for x in range(1, 9):
		t.forward(100)
		t.left(45)
	if filled == True:
		t.end_fill()
t.color(0.9, 0.75, 0) 
figura(t, True)
